// Converters module — DB row ↔ proto message conversions.
// Insight has no tables for MVP; all aggregation happens in-memory from Entry responses.
/// Placeholder for future DB row types if tables are added.
pub struct DummyRow;